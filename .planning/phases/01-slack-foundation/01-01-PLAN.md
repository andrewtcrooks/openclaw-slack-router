---
phase: 01-slack-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - .gitignore
  - .env.example
  - vitest.config.ts
  - src/config.ts
  - src/types.ts
  - subagent-config.json
  - src/config.test.ts
autonomous: true
requirements: [CFG-01, CFG-02, SLACK-04]

must_haves:
  truths:
    - "Service fails with a clear error message listing each missing/invalid env var when SLACK_BOT_TOKEN or SLACK_APP_TOKEN is absent"
    - "loadConfig() returns typed { SLACK_BOT_TOKEN, SLACK_APP_TOKEN } when valid env vars are set"
    - "SLACK_BOT_TOKEN must start with xoxb- and SLACK_APP_TOKEN must start with xapp- or validation fails"
    - "A subagent-config.json file exists with a routing table schema (defaultAgent + agents map)"
    - "SubagentConfig and SubagentEntry types are exported from src/types.ts"
  artifacts:
    - path: "src/config.ts"
      provides: "Env validation with zod, loadConfig() function"
      exports: ["loadConfig", "AppConfig"]
    - path: "src/types.ts"
      provides: "SubagentEntry and SubagentConfig interfaces"
      exports: ["SubagentEntry", "SubagentConfig"]
    - path: "subagent-config.json"
      provides: "Stub routing table with echo agent"
      contains: "defaultAgent"
    - path: "src/config.test.ts"
      provides: "Unit tests for config loading and validation"
      min_lines: 30
    - path: "vitest.config.ts"
      provides: "Test framework configuration"
    - path: "package.json"
      provides: "Project manifest with all dependencies"
      contains: "@slack/bolt"
    - path: "tsconfig.json"
      provides: "TypeScript compiler configuration"
      contains: "nodenext"
  key_links:
    - from: "src/config.ts"
      to: "zod"
      via: "z.object schema with safeParse"
      pattern: "z\\.object.*SLACK_BOT_TOKEN"
    - from: "src/config.ts"
      to: "process.env"
      via: "envSchema.safeParse(process.env)"
      pattern: "safeParse\\(process\\.env\\)"
---

<objective>
Initialize the openclaw-slack-router project scaffold and implement config/env validation.

Purpose: Establish the TypeScript project structure, install all dependencies, configure vitest, and build the config module that validates SLACK_BOT_TOKEN and SLACK_APP_TOKEN at startup. Also create the subagent routing table stub (CFG-01) and shared types.

Output: Working project with `npm run typecheck` passing, `npx vitest run` executing config tests, and a validated config module ready for the Bolt app in Plan 02.
</objective>

<execution_context>
@/Users/andrew/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-slack-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize project scaffold with all dependencies</name>
  <files>package.json, tsconfig.json, .gitignore, .env.example, vitest.config.ts</files>
  <read_first>
    - /Users/andrew/git/openclaw-slack-router/.planning/phases/01-slack-foundation/01-RESEARCH.md (Standard Stack section for exact versions, tsconfig.json and package.json examples)
    - /Users/andrew/git/openclaw/src/slack/monitor/provider.ts (lines 1-45, ESM/CJS import pattern for @slack/bolt)
  </read_first>
  <action>
Create the project root files:

1. **package.json** — Create with:
   - `"name": "openclaw-slack-router"`
   - `"type": "module"` (ESM)
   - `"version": "0.1.0"`
   - `"private": true`
   - Scripts: `"dev": "tsx watch src/index.ts"`, `"build": "tsc"`, `"start": "node dist/index.js"`, `"typecheck": "tsc --noEmit"`, `"test": "vitest run --reporter=verbose"`
   - Dependencies: `@slack/bolt` (^4.6.0), `zod` (^3.24.0), `dotenv` (^16.5.0)
   - DevDependencies: `typescript` (^5.8.0), `tsx` (^4.21.0), `@types/node` (latest), `vitest` (^3.0.0)

2. **tsconfig.json** — Create with:
   - `"target": "ES2022"`, `"module": "nodenext"`, `"moduleResolution": "nodenext"`
   - `"outDir": "dist"`, `"rootDir": "src"`, `"strict": true`
   - `"esModuleInterop": true`, `"skipLibCheck": true`, `"resolveJsonModule": true`
   - `"declaration": true`, `"sourceMap": true`
   - `"include": ["src"]`, `"exclude": ["node_modules", "dist"]`

3. **vitest.config.ts** — Create with:
   ```typescript
   import { defineConfig } from "vitest/config";
   export default defineConfig({ test: { globals: false } });
   ```

4. **.gitignore** — Include: `node_modules/`, `dist/`, `.env`, `*.tsbuildinfo`

5. **.env.example** — Include:
   ```
   SLACK_BOT_TOKEN=xoxb-your-bot-token
   SLACK_APP_TOKEN=xapp-your-app-token
   ```

6. Run `npm install` to install all dependencies and generate package-lock.json.

7. Run `npx tsc --noEmit` to confirm TypeScript config is valid (will pass trivially with no src files yet).
  </action>
  <verify>
    <automated>cd /Users/andrew/git/openclaw-slack-router &amp;&amp; test -f package.json &amp;&amp; test -f tsconfig.json &amp;&amp; test -f vitest.config.ts &amp;&amp; test -d node_modules &amp;&amp; echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"type": "module"`
    - package.json contains `"@slack/bolt"` in dependencies
    - package.json contains `"vitest"` in devDependencies
    - tsconfig.json contains `"module": "nodenext"`
    - tsconfig.json contains `"strict": true`
    - vitest.config.ts exists and imports from "vitest/config"
    - .gitignore contains `node_modules/` and `dist/` and `.env`
    - .env.example contains `SLACK_BOT_TOKEN=xoxb-` and `SLACK_APP_TOKEN=xapp-`
    - node_modules/ directory exists (npm install succeeded)
  </acceptance_criteria>
  <done>Project scaffold complete: package.json with all deps, tsconfig.json with nodenext ESM, vitest configured, .gitignore and .env.example in place, npm install successful.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Config module, types, subagent-config stub, and tests</name>
  <files>src/config.ts, src/types.ts, subagent-config.json, src/config.test.ts</files>
  <read_first>
    - /Users/andrew/git/openclaw-slack-router/.planning/phases/01-slack-foundation/01-RESEARCH.md (Pattern 5: Startup Config Validation, Code Examples section for config.ts and types.ts)
    - /Users/andrew/git/openclaw-slack-router/package.json (verify "type": "module" is set)
    - /Users/andrew/git/openclaw-slack-router/tsconfig.json (verify module settings)
  </read_first>
  <behavior>
    - loadConfig() with valid SLACK_BOT_TOKEN (xoxb-xxx) and SLACK_APP_TOKEN (xapp-xxx) in process.env returns { SLACK_BOT_TOKEN: "xoxb-xxx", SLACK_APP_TOKEN: "xapp-xxx" }
    - loadConfig() with missing SLACK_BOT_TOKEN throws Error containing "SLACK_BOT_TOKEN"
    - loadConfig() with missing SLACK_APP_TOKEN throws Error containing "SLACK_APP_TOKEN"
    - loadConfig() with SLACK_BOT_TOKEN not starting with "xoxb-" throws Error containing "xoxb-"
    - loadConfig() with SLACK_APP_TOKEN not starting with "xapp-" throws Error containing "xapp-"
    - loadConfig() with both tokens missing throws Error containing both field names
    - loadSubagentConfig() loads and parses subagent-config.json, returns valid SubagentConfig
  </behavior>
  <action>
**Write tests FIRST (RED), then implement (GREEN).**

1. **src/config.test.ts** — Create tests using vitest (`import { describe, it, expect } from "vitest"`):
   - Test group "loadConfig":
     - "returns typed config when valid env vars set" — Set process.env.SLACK_BOT_TOKEN="xoxb-test" and process.env.SLACK_APP_TOKEN="xapp-test", call loadConfig(), expect result to have both properties
     - "throws when SLACK_BOT_TOKEN is missing" — Delete process.env.SLACK_BOT_TOKEN, expect loadConfig() to throw with message containing "SLACK_BOT_TOKEN"
     - "throws when SLACK_APP_TOKEN is missing" — Delete process.env.SLACK_APP_TOKEN, expect loadConfig() to throw with message containing "SLACK_APP_TOKEN"
     - "throws when SLACK_BOT_TOKEN has wrong prefix" — Set to "bad-token", expect throw containing "xoxb-"
     - "throws when SLACK_APP_TOKEN has wrong prefix" — Set to "bad-token", expect throw containing "xapp-"
   - Test group "loadSubagentConfig":
     - "loads subagent config from file" — Call loadSubagentConfig(), expect result to have defaultAgent="echo" and agents.echo to exist
   - Use beforeEach/afterEach to save and restore process.env values

2. Run tests — they MUST fail (RED).

3. **src/types.ts** — Create:
   ```typescript
   export interface SubagentEntry {
     name: string;
     description: string;
   }

   export interface SubagentConfig {
     defaultAgent: string;
     agents: Record<string, SubagentEntry>;
   }
   ```

4. **subagent-config.json** — Create:
   ```json
   {
     "defaultAgent": "echo",
     "agents": {
       "echo": {
         "name": "echo",
         "description": "Echoes back the user's message (placeholder)"
       }
     }
   }
   ```

5. **src/config.ts** — Create:
   - Import `z` from "zod"
   - Import `readFileSync` from "node:fs"
   - Import `SubagentConfig` from "./types.js"
   - Define `envSchema = z.object({ SLACK_BOT_TOKEN: z.string().startsWith("xoxb-", "Must be a bot token (xoxb-)"), SLACK_APP_TOKEN: z.string().startsWith("xapp-", "Must be an app token (xapp-)") })`
   - Export `type AppConfig = z.infer<typeof envSchema>`
   - Export `function loadConfig(): AppConfig` — calls `envSchema.safeParse(process.env)`, on failure throws `new Error("Invalid configuration:\n" + formatted issues)`, on success returns `result.data`
   - Export `function loadSubagentConfig(path = "./subagent-config.json"): SubagentConfig` — reads file with readFileSync, JSON.parse, returns typed result
   - Use `.js` extension in all relative imports (ESM requirement with nodenext)

6. Run tests again — they MUST pass (GREEN).

7. Run `npx tsc --noEmit` — must pass with zero errors.
  </action>
  <verify>
    <automated>cd /Users/andrew/git/openclaw-slack-router &amp;&amp; npx vitest run src/config.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/config.ts exports `loadConfig` (grep: `export function loadConfig`)
    - src/config.ts exports `AppConfig` type (grep: `export type AppConfig`)
    - src/config.ts uses zod with startsWith("xoxb-") and startsWith("xapp-") validators
    - src/config.ts exports `loadSubagentConfig` (grep: `export function loadSubagentConfig`)
    - src/types.ts exports `SubagentEntry` interface (grep: `export interface SubagentEntry`)
    - src/types.ts exports `SubagentConfig` interface (grep: `export interface SubagentConfig`)
    - subagent-config.json contains `"defaultAgent": "echo"` and `"agents"` with `"echo"` entry
    - src/config.test.ts has tests for valid config, missing tokens, wrong prefixes, and subagent config loading
    - `npx vitest run src/config.test.ts` passes all tests
    - `npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <done>Config module validates env vars with zod (xoxb-/xapp- prefixes), types exported, subagent-config.json stub exists, all config tests pass, TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
1. `npx vitest run` — all config tests pass
2. `npx tsc --noEmit` — zero TypeScript errors
3. `grep -q '"type": "module"' package.json` — ESM mode confirmed
4. `grep -q 'export function loadConfig' src/config.ts` — config module exports present
5. `grep -q 'export interface SubagentConfig' src/types.ts` — types exported
6. `test -f subagent-config.json` — routing table stub exists
</verification>

<success_criteria>
- All dependencies installed (node_modules exists, package-lock.json generated)
- TypeScript compiles with zero errors
- Config module validates SLACK_BOT_TOKEN (xoxb-) and SLACK_APP_TOKEN (xapp-) from process.env
- Missing or malformed env vars produce clear error messages
- SubagentConfig type and subagent-config.json stub exist for Phase 2
- All vitest tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-slack-foundation/01-01-SUMMARY.md`
</output>
